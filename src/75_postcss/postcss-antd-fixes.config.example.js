/**
 * postcss-antd-fixes 示例配置（Ant Design + Tailwind 场景）
 *
 * 使用：复制为项目根目录 postcss.config.js，或合并进现有配置
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    'postcss-antd-fixes': {},
    autoprefixer: {},
  },
};
